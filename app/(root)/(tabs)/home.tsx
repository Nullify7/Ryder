import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Home = () => {
  return (
    <SafeAreaView>
      <Text className="text-4xl font-black">Home</Text>
    </SafeAreaView>
  );
};

export default Home;
